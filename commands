# get a remote repo
git clone git@github.com:OliWenman/geant4tomosim.git

# check what i have changed
git status

# add files to the repo
git add .gitignore

# commit those changes to the repository
git commit -m "Added gedit to gitignore"
 
# see whats happening
gitk

# put my changes on github
git push

# get changes off github
git pull

#To merge branch with master copy
#move to master
git checkout master

#update
git pull

#merge in the new branch
git merge new_branch

#there should be no problems if you have merge master in first, so just push back up
git push 
