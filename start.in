#Macro file for X-Ray simulation

#Initialize kernel
/run/initialize

#Choose if you would like to track the number of events happening (0 = no information until end of run, 1 = Events, 2 = Events and hits)
#/hits/verbose 1

#Choose how many photons to use
/run/beamOn 100
